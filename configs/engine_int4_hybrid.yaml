# INT4 hybrid_gpu bench 用 (与 engine_int4.yaml 不同 config; 不污染默认配置)
model:
  path: models/Qwen3.5-4B.int4.qlwc
  dtype: int4
  tokenizer_dir: models/Qwen3.5-4B-hf
mode: hybrid_gpu
tiers:
  gpu_vram_gb: 7
decode:
  spec_k: 3
  max_seq: 16384
  max_new_tokens: 2048
